# Copy to .env.local for local development.
# All TANIK client env vars are NEXT_PUBLIC_*-prefixed because the API base
# URL ships to the browser at build time.

NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
